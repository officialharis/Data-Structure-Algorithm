class Solution {
    public boolean isPalindrome(String s) {
        
        // Convert the string to lowercase and remove all non-alphanumeric characters
        // This ensures we only compare letters and numbers, ignoring spaces and symbols
        s = s.toLowerCase().replaceAll("[^A-Za-z0-9]" , "");

        // Initialize two pointers:
        // i starts from the beginning
        // j starts from the end
        int i = 0; 
        int j = s.length() - 1;

        // Loop until the two pointers meet or cross
        while (i <= j) {

            // If characters at current positions are not equal,
            // the string is NOT a palindrome
            if (s.charAt(i) != s.charAt(j)) {
                return false;
            } 
            else {
                // Move both pointers towards the center
                i++;
                j--;
            }
        }


⏱️ Time Complexity

toLowerCase() → O(n)

replaceAll() → O(n) (regex scan through string)

Two-pointer loop → O(n)

👉 Overall Time Complexity: O(n)
👉 Space Complexity: O(n) (because a new cleaned string is created)

🔍 Example Dry Run
Input:
"A man, a plan, a canal: Panama"
Step 1: Preprocessing
s = "amanaplanacanalpanama"
Step 2: Two Pointer Check
i	j	s[i]	s[j]	Match?
0	20	a	a	✅
1	19	m	m	✅
2	18	a	a	✅
3	17	n	n	✅
...	...	...	...	...
10	10	a	a	✅

Pointers keep moving inward.

Final Result:

✔ All characters match
👉 Output: true

❌ Another Example
Input:
"race a car"
After cleaning:
"raceacar"

Comparison:

r == r ✅

a == a ✅

c != e ❌ → return false
        // If all characters matched, the string is a palindrome
        return true;
    }
}
